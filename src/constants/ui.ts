export enum VariantType {
  Standard = 'standard',
  Outlined = 'outlined',
}

export enum ColorType {
  Primary = 'primary',
  Secondary = 'secondary',
}

export enum SizeType {
  Small = 'small',
  Medium = 'medium',
  Large = 'large',
}
